{
  "name": "anonymous-voting-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/src/server.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build && mv build ../backend/build"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "anonymous-voting-root": "file:",
    "axios": "^1.9.0",
    "backend": "file:",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}
